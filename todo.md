# TODO

- [ ] Entity/device docs in the UI. HA has no inline per-entity description
  field — only the manifest `documentation` link (and an optional device-page
  `configuration_url`). Deferred until there's an inline option; otherwise write
  a `docs/LEAK.md` entity guide (In Alert vs Alarm vs Leak, wet/dry mode, Sync
  Time) and point the link at it.
